import React, { useState, useEffect } from 'react';

const LevelSelector = () => {

    return (
        <div className='level-selector-container'>
            <p>This component allows the user to select the level they wish to play at.</p>
        </div>
    );
};

export default LevelSelector;